import styled from '@emotion/styled';
import img from 'images/caller.png';

export const MainContainer = styled.div`
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  background: url(${img}) no-repeat right -100px top 0px;
  background-size: 700px auto;
`;
